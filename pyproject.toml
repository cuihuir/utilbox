[project]
name = "png2ico"
version = "0.1.0"
description = "A modern GUI tool to convert images to ICO format"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "customtkinter>=5.2.0",
    "pillow>=10.0.0",
]

[project.optional-dependencies]
dev = [
    "pyinstaller>=6.0.0",
]
